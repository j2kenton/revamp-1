# Architecture Diagram Generator

You are a Principal Architect. Generate a high-level **Mermaid Flowchart (`graph TD`)** based on our high-level plan.

**Context Files:**

1. `@architecture-plan.md`

**Task:**
Map the complete user flow for **"User Authentication."**

* Start at the "User visits the site."
* Show the path for a new user signing up.
* Show the path for an existing user logging in.
* Show all decision points (e.g., "Is user logged in?", "Forgot password?").
* End at "User is on the main Dashboard."
